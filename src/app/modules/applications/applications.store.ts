import { ApplicationsState } from './applications.state';
import { ActionReducerMap } from '@ngrx/store';

/**
 * Applications store.
 */
export const applications: ActionReducerMap<ApplicationsState> = {
};
